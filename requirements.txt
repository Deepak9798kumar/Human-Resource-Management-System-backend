fastapi==0.109.0
uvicorn[standard]==0.27.0
# Use plain pydantic and install email-validator separately to avoid building
# pydantic extras which can pull in packages that require a Rust toolchain
pydantic==2.5.3
email-validator==1.3.1
python-multipart==0.0.6
